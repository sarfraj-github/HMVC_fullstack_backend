const DB = require("../../../config/database");

const schema = '';
const initSchema = (schemaName) => {
    this.schema = schemaName;
}

const createGroupWithPermission = async (payload) => {
    try {

    } catch (error) {
        throw error;
    }
}

const updateGroupWithPermission = async (payload, user_id) => {
    try {

    } catch (error) {
        throw error;
    }
}

module.exports = {
    initSchema,
    createGroupWithPermission,
    updateGroupWithPermission
}